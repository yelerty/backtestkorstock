# 자동화 시스템 로드맵

## 전체 시스템 아키텍처

```
[전체 2,500개 KOSPI/KOSDAQ 종목]
    ↓
① 1차 필터: 유동성 기준
   - 거래대금 > 50억원
   - 종가 > 5,000원
   - 제외: ETF, 관리종목, 신규상장 30일 이내
    ↓ ~1,500개
② 2차 필터: Money Analysis
   - 거래대금 급증 (20일 평균 × 1.5배 이상)
   - 거래량 급증 (20일 평균 × 1.5배 이상)
    ↓ ~300개
③ 3차: Trend Analysis
   - 이평선 배열 분석
   - 골든크로스 감지
   - 강도 점수화
    ↓ ~150개
④ 4차: S/R Analysis
   - 지지/저항 자동 계산
   - 돌파/눌림 패턴 감지
   - Position 점수화
    ↓ ~80개
⑤ 5차: Pattern Engine
   - 돌파형 패턴
   - 눌림목 패턴
   - 반등형 패턴
    ↓ ~50개
⑥ 6차: Score Engine
   - Trend Score (0~40점)
   - Money Score (0~35점)
   - Position Score (0~25점)
   - Final Score (0~100점)
    ↓ ~20개 (70점 이상)
    ↓ ~5개 (85점 이상)
⑦ AI 설명 생성
   - 차트 분석 요약
   - 매수/제외 근거
   - 진입가/손절/목표가
    ↓
[👤 사람의 최종 판단]
    ↓
[매수 버튼 클릭 또는 제외]
    ↓
[자동 위험관리: 손절/익절/트레일링]
```

---

## Phase 1: 분석 자동화 (매일 장 마감 후)

### 목표
매일 오후 16:00 (장 마감 후) 자동으로 전체 종목 분석 → TOP 20 종목 리스트 생성

### 1.1 데이터 수집

**필요 데이터:**
- 종목별 현재가, 시가, 고가, 저가, 종가
- 5/10/20/60/120일 이동평균
- 거래량, 거래대금
- 52주 고점/저점
- RSI(14)
- 수급 데이터 (기관, 외국인, 개인)
- 공시/뉴스 (선택)

**데이터 소스:**
- FinanceDataReader (종가, 거래량)
- yfinance (KOSPI 지수)
- KRX 공시시스템 (뉴스)
- 증권사 API (수급, RSI - 향후)

**스크립트:** `src/data_collector.py`

### 1.2 필터 1단계: 유동성 기준

```python
# 조건:
유동성_필터 = (
    거래대금 > 50억원 AND
    종가 > 5,000원 AND
    종목명 NOT IN 제외_목록
)

# 제외 목록:
제외_목록 = [
    "ETF", "REIT",  # 상품
    "관리종목",     # 유가증권
    신규상장_30일_이내,
    "따옴표 포함", # 우선주 등
]

# 결과: 1,500개 → 1,200개
```

**스크립트:** `src/filters/liquidity_filter.py`

### 1.3 필터 2단계: Money Analysis

#### 거래대금 분석

```python
거래대금_점수(종목):
    오늘_거래대금 = 오늘 거래대금
    평균20일_거래대금 = 20일 평균 거래대금
    
    배율 = 오늘_거래대금 / 평균20일_거래대금
    
    IF 배율 >= 3.0:
        점수 = 100 (매우 강함)
    ELIF 배율 >= 2.0:
        점수 = 85
    ELIF 배율 >= 1.5:
        점수 = 70
    ELIF 배율 >= 1.0:
        점수 = 50
    ELSE:
        점수 = 0 (필터 제외)
    
    RETURN 점수, 배율
```

#### 거래량 분석

```python
거래량_점수(종목):
    오늘_거래량 = 오늘 거래량
    평균20일_거래량 = 20일 평균 거래량
    
    배율 = 오늘_거래량 / 평균20일_거래량
    
    # 양봉/음봉 확인
    IF 종가 > 시가:
        양봉_계수 = 1.0
    ELSE:
        양봉_계수 = 0.7
    
    기본_점수 = MAP(배율, [1.0, 3.0], [0, 100])
    점수 = 기본_점수 × 양봉_계수
    
    RETURN 점수, 배율, 양봉_계수
```

#### Money Score 통합

```python
Money_Score = (
    거래대금_점수 × 0.5 +
    거래량_점수 × 0.5
)

# 최소 임계값: 40점 이상만 통과
IF Money_Score < 40:
    FILTER_OUT 종목
```

**스크립트:** `src/filters/money_filter.py`

### 1.4 분석 3단계: Trend Analysis

#### 이평선 배열 점수

```python
MA_배열_점수(종목):
    MA5, MA10, MA20, MA60, MA120 = 종목의 이평선
    
    # 1. 배열 구조 점수 (0~20)
    IF MA5 > MA10 > MA20 > MA60 > MA120:
        배열_점수 = 20 (완벽 정배열)
    ELIF MA5 > MA10 > MA20 > MA60:
        배열_점수 = 16
    ELIF MA5 > MA10 > MA20:
        배열_점수 = 12
    ELIF MA5 > MA10:
        배열_점수 = 8
    ELSE:
        배열_점수 = 0 (역배열)
    
    # 2. 방향 점수 (0~20)
    기울기_5 = (MA5_오늘 - MA5_5일전) / MA5_5일전
    기울기_20 = (MA20_오늘 - MA20_5일전) / MA20_5일전
    기울기_60 = (MA60_오늘 - MA60_5일전) / MA60_5일전
    
    IF 기울기_5 > 1% AND 기울기_20 > 0.5% AND 기울기_60 > 0%:
        방향_점수 = 20 (강한 상승)
    ELIF 기울기_5 > 0% AND 기울기_20 > 0%:
        방향_점수 = 15
    ELIF 기울기_5 > 0%:
        방향_점수 = 10
    ELSE:
        방향_점수 = 0
    
    Trend_Score = 배열_점수 + 방향_점수
    RETURN Trend_Score (0~40)
```

#### 골든크로스 감지

```python
골든크로스_확인(종목):
    # 지난 5일 이내 발생 여부
    IF (MA5_현재 > MA20_현재) AND (MA5_5일전 < MA20_5일전):
        골든크로스_발생 = True
        일수 = 발생_일수
        
        # 거래량 확인
        거래량_배율 = (발생_당시_거래량 / 거래량_평균)
        
        RETURN {
            발생: True,
            일수: 일수,
            거래량_확인: 거래량_배율 > 1.5
        }
    ELSE:
        RETURN {발생: False}
```

**스크립트:** `src/analysis/trend_analysis.py`

### 1.5 분석 4단계: Support/Resistance Analysis

#### 지지선 자동 계산

```python
지지선_탐지(종목):
    # 후보 지지선 목록
    지지_후보 = [
        고점(최근_20일),
        MA20,
        MA60,
        MA120,
        저점(52주),
        거래량_집중_존(volume_profile),
    ]
    
    # 각 지지선의 강도 점수
    FOR 지지 IN 지지_후보:
        강도_점수 = 계산_강도(지지)
        # 강도: 터치 횟수, 거래량, 보합 기간 등
    
    # 상위 3개 지지선만 선택
    상위_지지 = 상위_N개(지지_후보, N=3, 정렬_기준=강도_점수)
    
    # Zone으로 표현 (점수가 높을수록 넓음)
    FOR 지지 IN 상위_지지:
        zone_range = 지지 ± (지지 × 1%)
    
    RETURN 상위_지지[{가격, 강도, zone}]
```

#### 저항선 자동 계산

```python
저항선_탐지(종목):
    # 후보 저항선 목록
    저항_후보 = [
        고점(최근_20일),
        고점(52주),
        MA5,
        MA10,
        거래량_집중_존,
        이전_돌파선,
    ]
    
    # 지지선과 동일한 로직
    상위_저항 = 상위_N개(저항_후보, N=3, 정렬_기준=강도_점수)
    
    RETURN 상위_저항[{가격, 강도, zone}]
```

#### Position Score 계산

```python
Position_Score(종목, 현재가):
    지지 = 상위_지지[0]
    저항 = 상위_저항[0]
    
    # 1. 지지와의 거리 (0~15)
    거리_지지 = (현재가 - 지지) / 지지 × 100
    
    IF 거리_지지 <= 2%:
        거리_지지_점수 = 15 (지지 근처)
    ELIF 거리_지지 <= 5%:
        거리_지지_점수 = 12
    ELIF 거리_지지 <= 10%:
        거리_지지_점수 = 8
    ELSE:
        거리_지지_점수 = 0
    
    # 2. 저항과의 거리 (0~10)
    거리_저항 = (저항 - 현재가) / 현재가 × 100
    
    IF 거리_저항 >= 8%:
        거리_저항_점수 = 10 (여유 충분)
    ELIF 거리_저항 >= 5%:
        거리_저항_점수 = 8
    ELIF 거리_저항 >= 2%:
        거리_저항_점수 = 5
    ELSE:
        거리_저항_점수 = 0 (저항 너무 가까움)
    
    Position_Score = 거리_지지_점수 + 거리_저항_점수
    RETURN Position_Score (0~25)
```

**스크립트:** `src/analysis/support_resistance.py`

### 1.6 분석 5단계: Pattern Engine

#### 돌파형 패턴

```python
돌파형_탐지(종목):
    지난_50일_저항 = 고점(50일)
    현재가 = 오늘_종가
    
    # 조건 1: 저항선에 접근했는가? (95% 이상)
    IF 현재가 >= 지난_50일_저항 × 0.95 AND 현재가 < 지난_50일_저항:
        접근_상태 = True
    ELSE:
        RETURN {패턴: None}
    
    # 조건 2: 거래대금이 증가했는가?
    거래대금_배율 = 오늘_거래대금 / 평균_거래대금_20일
    IF 거래대금_배율 < 1.3:
        RETURN {패턴: None}
    
    # 조건 3: 저항 돌파했는가?
    IF 현재가 > 지난_50일_저항:
        돌파_확인 = True
        돌파_강도 = (현재가 - 지난_50일_저항) / 지난_50일_저항 × 100
        
        # 조건 4: 종가가 고가 근처인가? (고가의 75% 이상)
        IF 종가 >= 고가 × 0.75:
            신뢰도 = 95
        ELIF 종가 >= 고가 × 0.50:
            신뢰도 = 75
        ELSE:
            신뢰도 = 50
        
        RETURN {
            패턴: "돌파형",
            돌파_가격: 지난_50일_저항,
            현재_가격: 현재가,
            돌파_강도: 돌파_강도,
            신뢰도: 신뢰도
        }
    ELSE:
        RETURN {패턴: None}
```

#### 눌림목 패턴

```python
눌림목_탐지(종목):
    # 조건: 상승 추세 + 조정 + 지지 확인
    
    # 1. 상승 추세 확인 (지난 20일 고점 갱신)
    최근_고점 = 고점(20일)
    IF (고점(30일) <= 최근_고점):
        상승_추세 = True
    ELSE:
        RETURN {패턴: None}
    
    # 2. 조정 확인 (고점에서 하락)
    조정율 = (최근_고점 - 현재가) / 최근_고점 × 100
    IF 조정율 < 2% OR 조정율 > 15%:
        RETURN {패턴: None}
    
    # 3. 거래량 감소 확인 (조정 중)
    평균_거래량_조정중 = 평균(거래량[마지막_3일])
    평균_거래량_전체 = 평균(거래량[20일])
    IF 평균_거래량_조정중 > 평균_거래량_전체 × 0.9:
        RETURN {패턴: None}
    
    # 4. 지지선 확인
    주요_지지 = MA20 또는 이전_돌파선
    현재_거리_지지 = (현재가 - 주요_지지) / 주요_지지 × 100
    IF 현재_거리_지지 > 5%:
        RETURN {패턴: None}
    
    # 5. 거래량 증가 신호 (지지에서 반등)
    오늘_거래량 = 최근_거래량
    평균_거래량 = 평균(거래량[20일])
    IF 오늘_거래량 < 평균_거래량 × 1.5:
        신호_강도 = "약함"
    ELSE:
        신호_강도 = "강함"
    
    RETURN {
        패턴: "눌림목",
        고점: 최근_고점,
        조정율: 조정율,
        지지: 주요_지지,
        신호_강도: 신호_강도
    }
```

**스크립트:** `src/analysis/pattern_engine.py`

### 1.7 분석 6단계: Score Engine

#### 종합 점수 계산

```python
최종_점수_계산(종목):
    Trend_Score = 이평선_점수(종목)      # 0~40
    Money_Score = 거래대금_거래량(종목)  # 0~35
    Position_Score = 지지저항_점수(종목) # 0~25
    
    # 패턴 보너스
    패턴_보너스 = 0
    IF 돌파형_패턴:
        패턴_보너스 += 5
    IF 눌림목_패턴:
        패턴_보너스 += 3
    
    Final_Score = (
        Trend_Score +
        Money_Score +
        Position_Score +
        패턴_보너스
    )
    
    # 최대값 보정
    Final_Score = MIN(Final_Score, 100)
    
    RETURN {
        Trend: Trend_Score,
        Money: Money_Score,
        Position: Position_Score,
        Bonus: 패턴_보너스,
        Final: Final_Score
    }
```

#### 필터 기준

```python
# 매수 후보 조건
IF Final_Score >= 70:
    분류 = "매수 후보"
ELIF Final_Score >= 55:
    분류 = "관찰 대상"
ELSE:
    분류 = "제외"

# 추가 제외 조건 (아무리 점수 높아도)
IF 다음_중_하나():
    제외_종목 = True
    
    - 손절_기준_포함_수익률 < -5% (손절폭 너무 큼)
    - 저항_너무_가까움 (거리 < 1%)
    - 거래량_없는_상승 (거래량 배율 < 1.1)
    - 이평선_역배열 (5 < 60 AND 20 < 120)
    - RSI >= 80 (과매수)
    - 시가총액 < 1,000억원 (소형주)
```

**스크립트:** `src/analysis/score_engine.py`

### 1.8 결과 출력

```python
# 출력 포맷
결과 = {
    "작성_시간": "2026-08-06 16:00:00",
    "필터_통과_종목수": {
        "유동성": 1200,
        "거래대금": 300,
        "추세": 150,
        "지지저항": 80,
        "최종": 20
    },
    "상위_5개": [
        {
            "순위": 1,
            "종목명": "삼성전자",
            "종목코드": "005930",
            "현재가": 75000,
            "Final_Score": 87,
            "Trend": 35,
            "Money": 32,
            "Position": 20,
            "패턴": "돌파형",
            "진입가": 75500,
            "손절가": 74000,
            "목표가_1": 78000,
            "목표가_2": 80000,
            "설명": "(...AI 생성)"
        },
        # ...
    ]
}

# 저장 경로
- 파일: data/daily_analysis/{YYYY-MM-DD}.json
- 데이터베이스: SQLite (일별 히스토리)
```

**스크립트:** `src/output/result_formatter.py`

---

## Phase 2: 장중 실시간 감시 (09:00 ~ 15:30)

### 목표
Phase 1에서 선정된 TOP 20 종목의 실시간 가격, 거래량, 거래대금 감시

### 2.1 실시간 데이터 스트림

```python
# 1분마다 갱신
종목_감시[]:
    - 현재가
    - 누적 거래대금
    - 누적 거래량
    - 1분봉 시가/고가/저가/종가
    - 저항선까지의 거리
    - 거래대금 배율 (평균 대비)
    - 거래량 배율 (평균 대비)
```

### 2.2 돌파 신호 감지

```python
실시간_돌파_감지(종목):
    저항_가격 = 종목의_주요_저항
    현재가 = 실시간_가격
    
    # 조건: 저항선 돌파 + 거래량 + 거래대금
    IF (현재가 > 저항_가격):
        돌파_감지 = True
        
        # 거래량 확인
        현재_거래량_배율 = (현재_누적_거래량 / 평균_거래량 × 비례값)
        현재_거래대금_배율 = (현재_누적_거래대금 / 평균_거래대금 × 비례값)
        
        # 신뢰도 점수
        신뢰도 = 0
        IF 현재_거래량_배율 > 1.3:
            신뢰도 += 40
        IF 현재_거래대금_배율 > 1.5:
            신뢰도 += 40
        IF (현재가 > 저항_가격 × 1.02):
            신뢰도 += 20
        
        IF 신뢰도 >= 70:
            알림_전송(종목, "돌파 신호 감지", 신뢰도)
```

### 2.3 알림 채널

```python
알림_전송_채널 = [
    "텔레그램",    # 즉시성 최고
    "카카오톡",    # 실명 사용자
    "이메일",      # 기록용
]

# 알림 포맷
메시지 = f"""
🚀 {종목명} ({종목코드})

현재가: {현재가:,}원
저항선: {저항선:,}원
돌파폭: +{돌파폭:.2f}%

거래대금: {거래대금_배율:.1f}배
거래량: {거래량_배율:.1f}배

신뢰도: {신뢰도}점
지금 확인해보세요!
"""
```

**스크립트:** `src/realtime/breakout_monitor.py`

### 2.4 거짓 신호 필터

```python
# 다음의 경우 알림 무시
거짓신호_감지(종목):
    # 1. 스파이크 확인 (1분봉에서 튀었다가 바로 내려옴)
    IF (고가_1분봉 > 저항) AND (종가_1분봉 < 저항 × 0.99):
        무시 = True
    
    # 2. 거래량 부족한 돌파
    IF 거래량_배율 < 1.1 AND 거래대금_배율 < 1.2:
        무시 = True
    
    # 3. 시장이 급락 중
    IF KOSPI_낙폭 > 2%:
        무시 = True
    
    RETURN 무시
```

---

## Phase 3: 주문 자동화 (사람 승인 후)

### 목표
매수, 손절, 익절, 트레일링 자동화 (사람 최종 판단 후)

### 3.1 매수 주문

```python
자동_매수(종목, 승인=True):
    IF NOT 승인:
        RETURN "승인 필요"
    
    # 거래 파라미터
    진입가 = 종목의_계산된_진입가
    수량 = 계산_수량(계정_잔액, 포지션_사이징)
    
    # 주문 생성
    주문 = {
        종목코드: 종목,
        수량: 수량,
        가격: 진입가,
        주문_타입: "지정가"  # 또는 "시장가"
    }
    
    # 증권사 API 호출
    주문번호 = 증권사.주문(주문)
    
    # 기록
    DB_저장({
        주문번호,
        종목,
        매수가,
        수량,
        진입_시각,
        손절가,
        익절가,
        상태: "주문중"
    })
```

### 3.2 손절 주문

```python
자동_손절(포지션):
    손절가 = 포지션.손절가
    
    # 1. 손절가 도달 시 (항상 실행)
    IF 현재가 <= 손절가:
        주문 = {
            종목코드: 포지션.종목,
            수량: 포지션.수량,
            가격: 손절가,
            주문_타입: "지정가"
        }
        주문번호 = 증권사.주문(주문)
        포지션.상태 = "손절_주문"
    
    # 2. 강제 손절 (거래 불가 상황)
    IF 포지션.지속_시간 > 5시간:
        주문 = {
            종목코드: 포지션.종목,
            수량: 포지션.수량,
            주문_타입: "시장가"  # 무조건 팜
        }
        주문번호 = 증권사.주문(주문)
        포지션.상태 = "강제_손절"
```

### 3.3 익절 주문

```python
자동_익절(포지션):
    # 2단계 익절
    익절_1차 = 포지션.목표가_1
    익절_2차 = 포지션.목표가_2
    
    IF 포지션.이익율 >= 3%:
        # 1차: 50% 팔기
        주문_1 = {
            수량: 포지션.수량 × 0.5,
            가격: 익절_1차,
        }
        주문번호_1 = 증권사.주문(주문_1)
        포지션.수량_1차_익절 = 포지션.수량 × 0.5
    
    IF 포지션.이익율 >= 8%:
        # 2차: 나머지 50% 팔기
        주문_2 = {
            수량: 포지션.수량 - 포지션.수량_1차_익절,
            가격: 익절_2차,
        }
        주문번호_2 = 증권사.주문(주문_2)
```

### 3.4 트레일링 손절

```python
트레일링_손절(포지션):
    # 수익이 나면 손절선을 점진적으로 올림
    
    이익율 = (현재가 - 매수가) / 매수가 × 100
    
    IF 이익율 >= 5%:
        새로운_손절가 = 매수가 + (매수가 × 0.02)  # 매수가 +2%
        포지션.손절가 = MAX(포지션.손절가, 새로운_손절가)
    
    IF 이익율 >= 10%:
        새로운_손절가 = 매수가 + (매수가 × 0.05)  # 매수가 +5%
        포지션.손절가 = MAX(포지션.손절가, 새로운_손절가)
    
    IF 이익율 >= 15%:
        새로운_손절가 = 매수가 + (매수가 × 0.10)  # 매수가 +10%
        포지션.손절가 = MAX(포지션.손절가, 새로운_손절가)
```

### 3.5 포지션 사이징

```python
포지션_크기_계산(계정_잔액, 종목):
    # Kelly Criterion 기반
    
    # 1. 기본 계산
    1거래_최대손실 = 계정_잔액 × 0.02  # 한 거래당 2% 손실 한도
    
    # 2. 종목별 조정
    손절폭 = (매수가 - 손절가) / 매수가
    수량 = 1거래_최대손실 / (매수가 × 손절폭)
    
    # 3. 총 포지션 한도
    총_투입_자산 = 계정_잔액 × 0.5  # 최대 50% 투입
    
    IF (수량 × 매수가) > 총_투입_자산:
        수량 = 총_투입_자산 / 매수가
    
    RETURN 수량
```

---

## 구현 순서 (우선순위)

### Priority 1 (필수): Phase 1 기초
1. ✅ 데이터 수집 (`data_collector.py`)
2. ✅ 유동성 필터 (`liquidity_filter.py`)
3. ✅ Money 분석 (`money_filter.py`)
4. ✅ Trend 분석 (`trend_analysis.py`)
5. ✅ S/R 분석 (`support_resistance.py`)
6. ✅ Pattern 탐지 (`pattern_engine.py`)
7. ✅ Score 계산 (`score_engine.py`)

### Priority 2 (권장): Phase 1 자동화
1. 매일 16:00 자동 실행 (GitHub Action)
2. 결과 저장 (JSON + SQLite)
3. 웹 대시보드 (HTML)
4. AI 설명 생성

### Priority 3 (선택): Phase 2/3
1. 실시간 감시 (Websocket)
2. 알림 전송 (Telegram/카톡)
3. 거짓신호 필터
4. 증권사 API 연동
5. 자동 주문 (손절/익절)

---

## 데이터 저장 구조

```python
# 일별 분석 결과
data/daily_analysis/
    2026-08-06.json       # TOP 20
    2026-08-05.json
    2026-08-04.json
    ...

# 종목별 히스토리
data/stock_history/
    005930.csv            # 삼성전자 (OHLCV + 지표)
    000660.csv
    ...

# 포지션 기록
data/positions/
    positions.sqlite      # 매수/손절/익절 기록

# 이동평균선, S/R 등
data/indicators/
    {YYYYMMDD}_indicators.json
```

---

## 핵심 파라미터 (조정 가능)

| 항목 | 기본값 | 설명 |
|------|--------|------|
| 유동성_최소_거래대금 | 50억원 | 유동성 필터 |
| 거래대금_급증_기준 | 1.5배 | Money 필터 |
| 거래량_급증_기준 | 1.5배 | Money 필터 |
| MA_단기/중기/장기 | 5/20/120일 | Trend 분석 |
| 저항_접근_기준 | 95% | Pattern 감지 |
| 최소_점수_진입 | 70점 | Score 필터 |
| 최소_점수_관찰 | 55점 | Score 필터 |
| 손실_한도 | 2% | Risk 관리 |
| 익절_1차 | 3% | 익절 |
| 익절_2차 | 8% | 익절 |

---

## 다음 단계

이 문서를 기반으로 다음을 진행합니다:

1. **Phase 1 핵심 모듈 구현**
   - `src/` 디렉토리 구조 생성
   - 각 분석 모듈 Python 스크립트 작성
   - 테스트 및 파라미터 최적화

2. **GitHub Action 통합**
   - 매일 16:00 자동 실행
   - 결과 저장 및 커밋

3. **웹 대시보드**
   - 상위 20개 종목 표시
   - 실시간 차트
   - 상세 분석 페이지

4. **Phase 2 (선택사항)**
   - 실시간 감시 모듈
   - 알림 연동

5. **Phase 3 (향후)**
   - 증권사 API 연동
   - 자동 주문 (안정성 검증 필수)

---

**작성일:** 2026-08-06
**버전:** 1.0
**상태:** 설계 완료, 구현 대기
